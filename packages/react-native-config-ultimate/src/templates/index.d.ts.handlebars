// DO NOT COMMIT OR EDIT THIS FILE
// Regenerated by `npx rncu <env-file>`. Import `ConfigVariables` from this
// subpath in your TypeScript augmentation file (see README → TypeScript Setup).
export interface ConfigVariables {
{{#each @root}}
{{#if (isString this)}}
  {{@key}}: string;
{{/if}}
{{#if (isBoolean this)}}
  {{@key}}: boolean;
{{/if}}
{{#if (isNumber this)}}
  {{@key}}: number;
{{/if}}
{{/each}}
}
