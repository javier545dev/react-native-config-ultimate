// DO NOT COMMIT OR EDIT THIS FILE
export interface ConfigVariables {
{{#each @root}}
{{#if (isString this)}}
  {{@key}}: string;
{{/if}}
{{#if (isBoolean this)}}
  {{@key}}: boolean;
{{/if}}
{{#if (isNumber this)}}
  {{@key}}: number;
{{/if}}
{{/each}}
}

export type { ConfigValue, Spec } from './lib/typescript/src/index';

declare const UltimateConfig: ConfigVariables;

export default UltimateConfig;
