packages:
  - "packages/react-native-config-ultimate"
  # NOTE: Example apps under apps/ are intentionally EXCLUDED from the
  # workspace to test the library as if installed from npm (not via
  # workspace symlinks).
  # - "apps/example"      # RN 0.83 - excluded
  # - "apps/Example079"   # RN 0.79 - excluded
  # - "apps/example-web"  # Vite web - excluded
