name: Publish to npm

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: "Version being published (for reference only)"
        required: false
        default: "latest"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.x"
          registry-url: "https://registry.npmjs.org"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Run tests
        run: pnpm test

      - name: Copy LICENSE and README to package
        run: cp LICENSE README.md packages/react-native-config-ultimate/

      # TEMPORARY: Trusted Publishing (OIDC) returned a 404 on the first 0.2.2
      # publish attempt. Falling back to NPM_TOKEN so we can ship 0.2.2 now;
      # OIDC setup needs to be revisited as a follow-up before the next release.
      - name: Publish to npm
        run: npm publish --access public --provenance
        working-directory: packages/react-native-config-ultimate
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
